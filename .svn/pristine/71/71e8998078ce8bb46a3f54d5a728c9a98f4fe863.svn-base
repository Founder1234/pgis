/**
 * 图片样式类,用于存储要显示的icon的地图配置参数.例如:图片src,原始大小(canvas渲染缘故),缩放比例,定位描点等
 * @constructor
 * @param {Object} styleOptions - Icon 配置参数设置
 * @param {String} styleOptions.src	- 图片地址
 * @param {Number[]} styleOptions.size=[25,41] - 图片大小,默认为默认marker的图片大小
 * @param {Number[]} styleOptions.anchor=[0,0] - 默认图片左上角点
 * @param {String} styleOptions.anchorXUits='fraction' - anchorX值单位:'fraction'(比例,0~1,默认值),'pixels'(像素值,不超过图像X轴长)
 * @param {String} styleOptions.anchorYUits='fraction' - anchorY值单位:'fraction'(比例,0~1,默认值),'pixels'(像素值,不超过图像X轴长)
 * @param {Number} styleOptions.scale=1 - 图像缩放系数
 * @param {Number} styleOptions.opacity=1 - 图像透明度
 * @extends {olClass.style}
 */
var EzIcon = function(styleOptions){};
